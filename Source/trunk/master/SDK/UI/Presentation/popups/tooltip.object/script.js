// jQuery part
// change annotation from "$" to "jq"
var jq=jQuery.noConflict();

jq(document).one("ready", function() {
	
	jq(document).on('mousehover', 'data-tooltip', function(ev) {
		// Register listener
		// Remove data attribute
	});
});